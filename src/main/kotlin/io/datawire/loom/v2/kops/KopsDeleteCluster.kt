package io.datawire.loom.v2.kops

import io.vertx.core.logging.LoggerFactory


class KopsDeleteCluster(private val config: KopsContext) {

    private val logger = LoggerFactory.getLogger(javaClass)

    fun apply() {

    }
}